// This route is not compatible with `output: "export"` (GitHub Pages static HTML).
// Removed from static export by making it client-render-only placeholder.
// If you need a backend endpoint, deploy it to a server/Vercel instead.

export {};

